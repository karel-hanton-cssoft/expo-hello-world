{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/schemas/user.schema.json",
  "title": "User",
  "description": "User represents a plan participant. No authentication or global identity - users are per-plan labels only. User ID is NOT part of this structure - it's the dictionary key in Plan.users.",
  "type": "object",
  "required": ["displayName"],
  "properties": {
    "displayName": {
      "type": "string",
      "description": "Display name shown in UI",
      "minLength": 1
    },
    "firstName": {
      "type": "string",
      "description": "Optional first name for full name display"
    },
    "lastName": {
      "type": "string",
      "description": "Optional last name for full name display"
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "Optional email for contact/sharing (not globally unique)"
    },
    "phoneNumber": {
      "type": "string",
      "description": "Optional phone number for contact/sharing"
    }
  },
  "additionalProperties": false
}
